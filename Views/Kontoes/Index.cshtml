@model IEnumerable<mapkowanie.Models.Konto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    @{

        if (Model.Count() > 0)
        {
        }
        else
        {
            <h1><a asp-action="Create">Create New</a></h1>
        }

    }

</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Zablokowane)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RodzajKonta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WynagrodzenieMinimalne)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GodzinaStart)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GodzinaStop)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Adres)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nip)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Zablokowane)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RodzajKonta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WynagrodzenieMinimalne)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GodzinaStart)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GodzinaStop)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Adres)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nip)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
